# Cria pasta para comportar a infraestrutura
mkdir /infra

# Configura o git
git config --global init.defaultBranch main
git config --global user.name 'Servidor'
git config --global user.email servidor@domain.com
